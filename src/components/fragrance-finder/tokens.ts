/**
 * Tokens FragranceFinder — alignés sur OilProductCard / TrendCard.
 * Hex exacts (le projet utilise des styles inline + variables CSS).
 */
export const FF = {
  gold: "#C9A24A",
  goldDeep: "#A8801F",
  goldLight: "#E8C873",
  cream: "#FAF6EE",
  cream2: "#F6F0E4",
  ink: "#15110D",
  ink2: "#2C2620",
  border: "#E0D2B4",
  muted: "#8a7a5c",
  white: "#FFFFFF",
  display: "var(--font-display)",
  sans: "var(--font-sans)",
  // Halo de sélection (or)
  haloShadow: "0 0 0 3px rgba(201,162,74,0.45), 0 12px 28px rgba(168,128,31,0.22)",
} as const;
