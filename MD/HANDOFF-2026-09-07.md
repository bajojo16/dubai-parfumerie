# Handoff — session du 04→07/09/2026 (à lire après /compact)

Projet : maquette Next.js 16 `app/` de Dubaï Parfumerie. Dev sur **:3003** (jamais 3000). Branche de travail **`feature/bundle-3-pour-2`**, dernier commit poussé `a408da9`. `main` n'est PAS à jour : le `git fetch` m'est refusé par le classificateur → l'utilisateur doit lancer lui-même
`git fetch origin main && git checkout main && git merge --ff-only feature/bundle-3-pour-2 && git push origin main`.

## ⚠️ Agents EN COURS au moment du changement de modèle (07/09, ~07h30)
1. **Dupes** — enrichit `src/data/catalogue/dp-dupes.json` (349 produits boutique sans dupe) + ajoute des originaux dans `reference-perfumes.ts` ; règle forte = 2 sources, URL obligatoire ; ne touche pas `OLFACTIVE_TWINS`. À son rapport : vérifier slugs orphelins, `tsc`, `/preview-twin` 200, puis commiter.
2. **Propositions jumeau olfactif 1·2·3·4·5·6·9** (spec `MD/ameliorations-jumeau-olfactif-2026-09-07.md`, pas 7/8/10) + 4 défauts (Aventus vitrine≠recherche → `armaf-club-de-nuit`, description brute Sauvage, Alyssa/Baccarat badge, « très proche » forcé). Touche `OlfactiveTwin.tsx`, `olfactive-match.ts`, nouvelle route `/jumeau/[id]`, table `reference-prices.ts`. À son rapport : `tsc`, curl `/`, `/preview-twin`, `/jumeau/<id>`, puis commiter.
Les deux peuvent écrire dans `reference-perfumes.ts` : relire le fichier avant de commiter, vérifier qu'il compile.
Ne pas relancer d'agent en doublon ; `git status` montre ce qu'ils ont écrit.

## Agents terminés et commités : enchères `b0bd73f` → filtres `894a19b`, audit `521090e`, mur `92c94f7` → recherche/filtre par parfum `b173cb1`, logos, PDF propositions `a408da9`.

## Enchères — état livré (07/09)
Chrono rouge, dernier enchérisseur (initiales + pays) `4e963ce`. Recherche + tri + pastilles statut/état, URL `?q=&statut=&etat=&tri=`, lots **occasion** (Khamrah 75 %, 9PM 55 %) avec photo de niveau Higgsfield (`public/assets/auctions/*-niveau-01.webp`, 928×1152), `FillGauge.tsx`, `ConditionPanel` dans le modal, semis d'enchérisseurs déterministe (corrigeait une erreur d'hydratation) `894a19b`. Préexistant non touché : à 390 px le prix du modal passe sur deux lignes face au chrono.

## Dossier unique des fichiers Claude DP (07/09)
Les 4 dossiers (`Documents/DP/MD et fichiers Claude`, `DP NextCloud/MD`, `MD DP et fichiers Claude`, `MD et fichiers Claude`) sont fusionnés dans **`~/Library/CloudStorage/Nextcloud-31.97.45.102-ARBoy/DP NextCloud/MD DP`** — un sous-dossier par conversation, index `LISEZMOI.md`, doublons dans `_archive/fusion-2026-09-07/`. Chemins réécrits dans 15 masters. L'ancien chemin local est un **symlink** vers MD DP. Reste à faire par l'utilisateur : reconnecter les projets Claude.ai + le prompt de la tâche planifiée « Fiche Karim » sur le nouveau chemin.

## (historique) Agents lancés pendant la session
1. **Enchères** — crée `src/data/auctions.ts`, `src/components/auctions/*`, `src/app/[locale]/encheres/page.tsx`, ajoute `{ label: "Enchères", href: "/encheres" }` avant Bons Plans dans `Header.tsx`. Pendant son écriture, le navigateur a montré des erreurs transitoires (« async Client Component », « page couldn't load ») : c'est Turbopack qui sert un état intermédiaire. À son rapport : vérifier `/encheres` 200, `tsc`, puis **commiter** (`git add -A` sauf `MD/` si l'audit tourne encore).
2. **Audit complet** → `MD/audit-site-2026-09-07.md` + `.pdf` (SEO, GEO, CRO, juridique, a11y, i18n, analytics, données, feuille de route). Ne modifie rien hors `MD/`. À son rapport : relayer les 5 constats majeurs, commiter `MD/`.

## Décisions prises par l'utilisateur (ne pas rouvrir)
- Prix : médianes **marché français uniquement** (règle `REGLE-PRIX-FR.md`), réalignement si écart > 15 %, arrondi « ,90 » le plus proche. Arbitrages manuels dans `PRIX_IMPOSES` (`src/data/catalogue/dp-corrections-prix.ts`) : Al Shaikha Hind 90, Arwa 39, Bidun Esam ×2, Daam Watani **80**, Vanilla Voyage **49**, Jean Lowe Vibe **34,90** (validé sur le portail).
- Jumeau olfactif : la recherche recoupée prime — Salvo → **Sauvage Elixir**, Aventus → **Club de Nuit**, The Tux → **YSL Tuxedo**. Sauvage EDT → Urban Man Elixir (dupe documenté).
- Échantillons : **Reef seul** en stock, autres « Bientôt disponible » (`SAMPLE_BRANDS_AVAILABLE`).
- Bons Plans → ouvre `/offres/lot-3-pour-2` directement. Grille catalogue : min **3** par ligne.
- « Oud & Roses » s'écrit **« Oud and Roses »** dans le code (pas dans le JSON catalogue, source externe).
- Commande à la demande : le parfum d'abord (bloc A « Le parfum ou la marque »), maison = préférence pas mur, WhatsApp **recommandé**, pas de montant d'acompte (renvoi au devis), « Votre demande » avant « Le parcours ».
- Lattafa Yara dupe : sources divergentes → confiance faible, non servi.

## Architecture données produit (3 couches, dans `search-catalog.ts` → `collect()`)
1. `product-details.ts` — 25 fiches rédigées (gagnent la dédup).
2. `catalogue/dp-catalogue.json` + `catalogue-to-search.ts` — 110 fiches **vérifiées** (marque recoupée, pyramide sourcée, prix marché). Photos/descriptions relevées : `dp-fiches-site.json`.
3. `catalogue/dp-boutique.json` + `boutique-to-search.ts` — 349 fiches **non vérifiées** reprises de dubaiparfumerie.com (ld+json), **sans pyramide**, popularité 18. Site = **448 produits / 35 maisons**.
Dupes : `catalogue/dp-dupes.json` + `dp-dupes.ts` (50 servis ≥ moyenne), câblés dans `olfactive-match.ts` entre relecture manuelle et score. 14 originaux ajoutés à `reference-perfumes.ts` (LV, Kayali, Gissah…).
Images relevées : `public/assets/products/dp/<slug>/` (28 Mo). Slugs = `slugify(marque)-slugify(nom)`.

## Portail prix (artefact) — https://claude.ai/code/artifact/4d4acf12-4134-4678-a0bb-90cb3476d383
Source : `scratchpad/position-tarifaire.html` (head/body/script + `prix-slim.json`). DB : collection `prix/<id>` (prix validés) et `vues/<id>`. **Toujours relire le store** (`read_db`) avant de répondre sur un prix. Liens vers `localhost:3003/memo/catalogue-corrige.html` et `ecarts-de-prix.html` (copies dans `public/memo/`).

## Pièges rencontrés
- `grep -c … && python3` court-circuite si 0 match → édit non appliqué (arrivé deux fois). Vérifier avec `grep -c` après édition.
- next/image : pas de `?query` sur src local (400). Puces de maisons servies `unoptimized` depuis `public/brands/marks/` (51 puces détourées).
- Le fichier `product-details.ts` a été réécrit par une autre session sur Marshmallow Blush (lot Higgsfield 04/09) : poser ses changements par-dessus, ne pas restaurer.
- Nomenclature fichiers : `dp_parfumerie-<marque>-<produit>-<type>-NN.webp`, 1400 px q82. Flacons trop étirés (ratio < 0,5) → recadrer sur carré transparent (Noora, Tanasuk).
- Higgsfield : `medias[].role` = `"image"` ; importer les URLs CDN `api.dubaiparfumerie.com` via `media_import_url`. Visuels produits dans `public/assets/lots/`, `public/assets/boutique/`.

## Règle après compact
Hook `SessionStart` (matcher `compact`) dans `.claude/settings.json` du dossier parent injecte le HANDOFF le plus récent ; règle aussi dans `app/CLAUDE.md`. **Mettre ce fichier à jour avant chaque compact.**

## Audit du site — `MD/audit-site-2026-09-07.pdf` (30 p.)
5 constats majeurs : JSON-LD `InStock` en dur + `AggregateRating` fabriqué sur 448 fiches ; parcours d'achat factice (panier fiche inerte, /panier /checkout 404, 18/27 liens footer 404 dont 5 légales) ; SEO technique (pas de `lang`, sitemap 6/448, 154 previews indexables, titres dédoublés) ; 91 % des descriptions dupliquées de dubaiparfumerie.com ; contraste or 2,7:1. Correctifs P1 proposés en agents parallèles — en attente de l'accord utilisateur.

## À faire (file d'attente utilisateur)
- Correctifs P1 de l'audit (si accord).
- Doublon catalogue : `maison-asrar-vanilla-voyage` (couche boutique) a une fiche **404**, la vraie est `vanilla-voyage` (product-details) — la dédup marque|nom ne l'attrape pas.
- Mur des éloges : lien footer seulement (pas d'onglet header, décision utilisateur) ; avis Vanilla Voyage ajouté ; filtre `?produit=&q=`.
- Onglet **« Mur des éloges »** (`/mur-des-eloges`, page livrée) — attendre son signal.
- Recomposer l'**accueil** sur le top 10 hype (`MD/hype-reseaux-accueil.md`) : rails actuels Aurum/Summer/Volcano/Amber Nuit sans signal.
- Rédiger 4 fiches viraux sans fiche : Liquid Brun, La Rouge Baroque, Cocktail Intense, Ameerat Al Arab.
- `oils/silk.png` (ratio 0,31) à recadrer ; Salvo Elixir n'a que 2 relevés (23,90 €) ; Oud Royal du lot pointe `prod-6.jpg` (bukhoor).
- 19 maisons sans logo (liste dans le rapport logos) ; Ajmal/Hemani/Naseem = variantes discutables à valider.
- 3 erreurs TS préexistantes `_home-client.tsx` (~l.1094-1100, `Type 'null'`) + `next.config` formats.

## Format des réponses attendu (CLAUDE.md global)
`⏱ ~X` en tête · caveman (fragments, pas de remplissage) · fin : `── Actions ──` numérotées, `── Tâches ──` ✅🔄⏳, `── Contexte ──` 🪟 | 📅 | 🧠. Une action ⇢ l'utilisateur répond par numéro. Sub-agents pour les tâches lourdes ; résumer leur sortie.
